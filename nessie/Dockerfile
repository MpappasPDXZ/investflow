# Nessie REST Catalog Dockerfile
FROM ghcr.io/projectnessie/nessie:latest

# Nessie runs on port 19120 by default
EXPOSE 19120

# Use environment variables for configuration
ENV QUARKUS_HTTP_PORT=19120

# Default command (Nessie image already has entrypoint)
CMD []

