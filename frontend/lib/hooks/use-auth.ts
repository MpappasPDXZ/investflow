/**
 * Authentication hooks and utilities
 * Re-exports from auth-context for backward compatibility
 */
export { useAuth, AuthProvider } from '../contexts/auth-context';
export type { User } from '../contexts/auth-context';
